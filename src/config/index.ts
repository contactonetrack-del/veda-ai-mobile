/**
 * VEDA AI Design System - Unified Exports
 * Central export point for all design tokens
 */

// Colors
export {
    colors,
    brand,
    semantic,
    gradients,
    shadows,
    opacity,
    type ThemeColors,
} from './colors';

// Typography
export {
    fontFamily,
    fontSize,
    lineHeight,
    letterSpacing,
    fontWeight,
    typography,
    type TypographyVariant,
} from './typography';

// Spacing
export {
    spacing,
    borderRadius,
    touchTarget,
    iconSize,
    avatarSize,
    zIndex,
    layout,
    gap,
    type SpacingScale,
    type BorderRadiusScale,
} from './spacing';

// Animations
export {
    duration,
    easing,
    springConfig,
    timingConfig,
    transforms,
    delay,
    animationPresets,
    shimmerConfig,
    pulseConfig,
    type SpringConfigName,
    type TimingConfigName,
    type AnimationPresetName,
} from './animations';
